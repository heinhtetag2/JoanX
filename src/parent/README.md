# parent — the Parent app (guardian dashboard)

The calm guardian experience: onboarding/auth, add-child wizard, device pairing,
reports, and rules/settings.

Defines the pink **`BRAND`** palette (logo magenta `#E00477`, with `ink` for active
states) and the `ChoiceGroup` radio at the top of `ParentScreens.jsx`.
See `/ARCHITECTURE.md`.
