# 3D character references

Drop reference images here (PNG/JPG/renders/etc.) for the **3D (`toy`) mascot style**.

These feed the `MascotToy` renderer in `app/core/characters.jsx` — the soft-3D,
gradient-shaded buddy line selectable via the **3D** chip in the Tweaks panel.
