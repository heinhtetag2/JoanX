# shell — app frame

`App.jsx` is the iOS device frame, the Child/Parent role switcher, the router, and
the **Tweaks** panel. It loads **last** (it depends on every screen being defined).
See `/ARCHITECTURE.md`.
