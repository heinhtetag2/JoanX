# overview — design tooling (not shipped)

Renders every screen on one pan/zoom canvas for design review. Served by
`design/overview.html`, not by the app itself.

- `design-canvas.jsx` — the pan/zoom canvas
- `Overview.jsx` — lays out all screens on it
